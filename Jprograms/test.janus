//procedure encrypt1(int m)
//    local int k = 17
//    from k = 17 loop
//        //show(k)
//        m += k
//        k -= 1
//    until k = 0
//    delocal int k = 0
    
procedure encrypt(int m, int k)
    if k = 0 then
        // fine ricorsione
        // niente da fare
    else
        m += k
        k -= 1
        call encrypt(m, k)
        k += 1     // ripristino reversibile
        m -= k     // ripristino reversibile
    fi k = 0

procedure main()
    local int key = 17
    local int msg = 0
    show(msg)          // 0
    show(key)          // 17
    call encrypt(msg, key)
    show(msg)          // 153 (17+16+...+1)
    show(key)          // 17 (non modificata)
    uncall encrypt(msg, key)
    show(msg)          // torna 0
    show(key)          // torna 17
    delocal int msg = 0
    delocal int key = 17
